apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: shopping-cart
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: shopping-cart
  filterTags:
    pattern: "^shopping-cart-v(.*)$"
    extract: "$1"
  policy:
    semver:
      range: ">=0.0.0-0"
