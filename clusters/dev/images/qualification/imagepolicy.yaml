apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: qualification
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: qualification
  filterTags:
    pattern: "^qualification-v(.*)$"
    extract: "$1"
  policy:
    semver:
      range: ">=0.0.0-0"
